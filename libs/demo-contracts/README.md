# demo-contracts

The shapes the Demo catalogue puts on the wire: products, categories, reviews
and tags, with the request bodies that write them.

Demo, so an Adopter deletes this package along with `apps/aze-api/src/product/`
— see `docs/demo.md`. Nothing in the Platform imports it, and the module
boundary rule in `eslint.config.mjs` is what keeps that true.
